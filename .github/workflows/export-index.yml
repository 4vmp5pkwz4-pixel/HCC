name: Export exact index

on:
  push:
    branches:
      - feat/unified-atlas-time-4.152

permissions:
  contents: read

jobs:
  export-index:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify exact index blob
        run: |
          set -euo pipefail
          echo "commit=$(git rev-parse HEAD)"
          echo "index_blob=$(git hash-object index.html)"
          test "$(git hash-object index.html)" = "851a1903fd45c011da2e9d2329b34ca6a210fc7d"
      - name: Upload exact index
        uses: actions/upload-artifact@v4
        with:
          name: exact-index-html
          path: index.html
          if-no-files-found: error
          retention-days: 1
